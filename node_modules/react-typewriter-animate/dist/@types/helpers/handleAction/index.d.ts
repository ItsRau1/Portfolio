import { PackInfo } from "../../types";
import { ComposedTypewriterProps } from "../../Typewriter";
export declare const handleAction: (props: ComposedTypewriterProps, pack: PackInfo, moveOn: () => void) => void;
