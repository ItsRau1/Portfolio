import { PackInfo } from "../../types";
import { ComposedTypewriterProps } from "../../Typewriter";
export declare const handleDelete: (props: ComposedTypewriterProps, pack: PackInfo, moveOn: () => void, amount: number) => void;
