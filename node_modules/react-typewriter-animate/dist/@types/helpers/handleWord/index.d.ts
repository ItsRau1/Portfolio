import { PackInfo } from "../../types";
import { ComposedTypewriterProps } from "../../Typewriter";
export declare const handleWord: (props: ComposedTypewriterProps, pack: PackInfo, moveOn: () => void) => void;
